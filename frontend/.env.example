# PRODUCTION: leave blank — the frontend is served by the same Nginx that
# proxies /api/ and /socket.io/, so relative URLs work automatically.
VITE_API_URL=

# LOCAL DEV: point directly at the backend dev server
# VITE_API_URL=http://localhost:3000
